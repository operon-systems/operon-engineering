name: ci

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  minimal-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Engineering CI active"

  policy-compliance:
    uses: operon-systems/governance-policies/.github/workflows/policy-compliance.yml@main